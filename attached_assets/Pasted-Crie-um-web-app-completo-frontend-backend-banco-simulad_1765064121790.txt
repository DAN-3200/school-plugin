Crie um web app completo (frontend + backend + banco simulado) que funcione como um “plugin simulado” para um Ambiente Virtual de Aprendizagem (AVA). O nome do sistema deve ser SEI — Sistema de Engajamento e Intervenção Educacional.

OBJETIVO DO SISTEMA

O app deve simular um plugin educacional que integra três funcionalidades:

Dashboard de desempenho e risco escolar dos alunos

Módulo socioemocional com check-ins semanais

Sistema de intervenções pedagógicas e acompanhamento de progresso

O sistema deve permitir login de dois perfis:

Aluno

Professor/Coordenador

A autenticação pode ser simples (mock), sem banco real.
O banco pode ser: JSON local, SQLite simples, ou arrays em memória.

📌 FUNCIONALIDADES QUE DEVEM SER IMPLEMENTADAS
1. Dashboard do Professor

Criar uma página acessível somente ao professor contendo:

Dados por aluno:

nome

notas (mock)

presença (mock)

participação no AVA (quantidade de acessos, mock)

atrasos em atividades (mock)

resultado dos check-ins socioemocionais

Índice de Risco Escolar (IRE) calculado automaticamente

Lógica do índice de risco (IRE):

Use este cálculo simples:

IRE = (peso1 * baixa nota) +
      (peso2 * faltas) +
      (peso3 * baixa participação) +
      (peso4 * estresse alto) +
      (peso5 * desmotivação)


Classificação:

0–30 = Baixo risco (verde)

31–60 = Médio risco (amarelo)

61–100 = Alto risco (vermelho)

O dashboard deve exibir:

lista de alunos com cor do risco

gráficos ou indicadores simples (opcional, mas recomendado)

2. Módulo Socioemocional (Aluno)

Criar página para o aluno responder check-ins semanais com perguntas simples:

Exemplo:

Como está sua motivação esta semana? (0–10)

Seu nível de estresse? (0–10)

Como está seu foco? (0–10)

Como você avalia sua organização? (0–10)

Registrar as respostas e exibir histórico básico para o aluno.

Para o professor, exibir gráficos ou lista de respostas socioemocionais do aluno.

3. Sistema de Intervenções Pedagógicas

Na página do professor:

Permitir abrir uma intervenção para um aluno com risco médio/alto.

Campos:

objetivo da intervenção

ações planejadas

data de início

responsáveis

Permitir registrar acompanhamentos semanais:

observações

progresso (0–10)

No dashboard, mostrar:

intervenções abertas

intervenções concluídas

evolução do aluno

📁 ESTRUTURA DO PROJETO

Use algo como:

/backend
   server.js ou main.py
   routes/
   data/
   models/
/frontend
   React app
   pages/
   components/

🌐 REQUISITOS TÉCNICOS

Backend: Node.js (Express) OU Python (FastAPI/Flask)

Frontend: React + Tailwind ou Material UI

Comunicação via REST API

Nunca precisa autenticação real; apenas simule perfis

Inclua um script que popula o sistema com:

5 alunos

dados fictícios de notas, faltas e participação

respostas socioemocionais iniciais

🧪 REQUISITOS DE TESTE / DEMONSTRAÇÃO

O app deve funcionar localmente com:

página de login (mock)

página do aluno (check-in + histórico)

dashboard do professor

página de intervenções

Não precisa ser bonito, mas deve ser funcional.

🎯 ENTREGÁVEIS ESPERADOS

Ao finalizar, gere:

código completo

instruções de execução

documentação básica no README

exemplos de requisições da API

telas funcionais do sistema

👍 IMPORTANTE

O foco é criar um protótipo funcional, não um plugin real.
Ele deve simular funcionamento dentro de um AVA.

❗ POR FAVOR, AGORA GERE O PROJETO COMPLETO.